import { filterArticles } from 'bustle-clone/helpers/filter-articles';
import { module, test } from 'qunit';

module('Unit | Helper | filter articles');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterArticles([42]);
  assert.ok(result);
});
